# A76 核心端到端证据回执

## 环境与范围

- 日期：2026-08-18
- 模型：`glm-5-2-260617`，工具内隐藏思考关闭
- 用户事实输入：`我是开雪茄馆的，该怎么起号？`
- 运行终点：`AdaptedDraft`（`FormatDecision` 允许保持 provisional）
- 明确未运行：ProductionPlan、素材任务、MediaKit、平台发布
- 本地测试产物不提交；下列 SHA-256 只用于复核本轮原始回执

## 原始运行摘要

| 回执 | SHA-256 | 结果 |
| --- | --- | --- |
| 宠物殡葬留出 | `94551dde7f78be9b2513037cef2421036ebb2ad4d3a4bf76581535af5e511567` | 根停在宠物殡葬；无 TopicBrief，失败 |
| 雪茄完整根与研究 | `0c40285b53c8bbbb02256618f398472fc55338808ecebf7a608cb1cc8f1ea58e` | 根为雪茄；形成 Cohiba 题；下游 JSON 失败 |
| 结构恢复后半程 | `664f406ce18196e0957473f2f84c6506a00d633c33eba9c6e014fdf73940d688` | 九类产物落账；无 ProductionPlan；人工发现证据外人名 |
| 事实修复后交付 | `de0e56712fb3a40f587360feccb2d07024e497874c8d99a3658ab822049812e8` | 删除证据外人名和“全球最贵”；BaseDraft 成立 |
| 无制作表达续跑 | 台账产物 ID 见下 | FormatDecision 与 AdaptedDraft 成立；无 ProductionPlan / MediaArtifact |

## 阶段耗时

第一次完整雪茄运行：

```text
语义 + 内容根 + 地图：209.48 秒
研究 + TopicBrief：294.15 秒
孵化判断失败调用：84.51 秒
MessagePlan 失败调用：90.06 秒
```

统一结构恢复后的后半程：

```text
孵化判断：75.77 秒
MessagePlan + BaseDraft：41.62 秒
FormatDecision + AdaptedDraft：60.06 秒
```

事实护栏真实复跑：`83.96 秒`，包含一次有界语义修复。

事实修复后的形式与表达续跑：`75.23 秒`。

## 关键输出

```text
内容根：雪茄

TopicBrief：
Cohiba 如何从卡斯特罗的私人外交礼物变成全球高端雪茄品牌？
这一转变中，政治特权、产区风土与工艺稀缺性各自扮演了什么角色？

事实修复后标题：
Cohiba：一支雪茄如何从政治特权走向全球溢价

FormatDecision：provisional / spoken_delivery
FormatDecision 产物：artifact_93c27a214298d59003fb8465c6e27f444e3330b9f8a6515a8aef7dc1c56bc279
AdaptedDraft 产物：artifact_06dd9e8449af5c5a26aee37fa8f5cd8da485835ec2ac1031a2417444158edb1d
AdaptedDraft 单元：8
```

修复稿保留的证据限制：现有来源主要是百科摘要和零售商内容，缺少古巴政府档案、Habanos S.A.
官方文件或学术研究；起源故事和商业化过渡过程不能当成已被一手证据完全坐实。

形式决定明确记录：用户是否出镜、表达能力、平台和可用视觉资源均未知，因此口头表达只是候选，
不能视为已经确认的账号路线。适配稿人工复核未发现 BaseDraft 之外的新专名、数字或绝对断言。
最终台账产物类型为：

```text
content_reading / content_world / topic_brief / incubation_brief /
incubation_judgment / message_plan / draft_version / format_decision / adapted_draft
```

不存在 `production_plan` 或 `media_artifact`。

## 自动回归

```text
全部内容理解、孵化、形式、适配、制作合同与导入边界回归：333 passed, 1 warning
完整后端非 live 回归：12074 passed, 76 skipped, 17 warnings in 421.79s
```

回归覆盖顺序语义链、消费场景不能冒充更大内容世界、研究实体改名修复、畸形 JSON 有界恢复、
证据外专名/数字/绝对断言修复、项目停止边界和 MediaKit 导入隔离。第一次完整回归发现七个新增
内部提示标签尚未进入共享防伪名单；逐项归类并加入净化回归后，相关 180 项测试和第二次完整回归
均通过。17 条 warning 为既有依赖弃用和测试短密钥提醒，本轮无失败。
