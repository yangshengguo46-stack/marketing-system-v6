# A58 并行语义工作者验证回执

## 运行边界

- 日期：2026-08-17
- 模型：本机未跟踪配置中的 `glm-5-2-260617`
- 思考模式：关闭
- 运行时：第六版 `explore_content_world`
- 外部词义证据：按案例启用本地只读索引
- API Key、供应商响应原文和隐藏思考：未写入本回执

## 自动验证

并发屏障夹具会在两个首轮结构化工作者都进入调用后才放行。任一实现若改回串行，会在测试中超时，
因此本轮验证的是实际任务重叠，而不是代码形态。模型夹具使用不可变的逐 Schema 绑定，避免并发共享
状态让错误路径假通过。

删除旧串行路径后的最终聚焦套件结果：

```text
120 passed in 5.03s
```

Ruff 格式化、Ruff 检查和 `git diff --check` 均通过。

## 真实案例

### 拳击手套

```text
semantic lexical_head: 手套
modifier: 拳击 -> constitutive_context
context_bound_world: 拳击 / complete_activity / [拳击]
selected content_root: 拳击
elapsed: 101.44s
```

地图保留拳击技术、拳手、赛事、规则、历史、训练和装备等方向；“搏击”只作为跨界分支，未反向扩大根。
该案例通过。阶段耗时显示地图约 `61.84s`，是当前主要性能瓶颈。

### 黄金礼品

一次真实回归得到：

```text
lexical_head: 礼品
selected content_root: 礼赠
elapsed: 112.76s
```

地图已经进入送礼、回礼、关系边界和仪式，但没有完成“礼品 -> 礼 -> 人与人之间的相处与人情世故”。
该案例严格不通过。后续修正必须先加入通用失败测试与非黄金反例，再运行全新真实回归。

## 当前结论

并行架构在工程和一个非黄金真实案例上成立，证明可以减少串行阶段并保留唯一裁决；它尚未证明营销
语义质量已经完成。用户决定将其作为当前可用检查点，主线转入具体 TopicBrief。
